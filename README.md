# Unified-Security-Dashboard-Flask-React-
 full‑stack web dashboard that aggregates alerts and statistics from all other projects. Uses Flask for the backend and a modern frontend with Chart.js. Real‑time updates via SocketIO (or polling).
